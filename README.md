A primitive "flashlight" for Android. It simply sets the screen brightness to full, and activates the camera's flash in ["torch mode"][1].

  [1]:http://developer.android.com/reference/android/hardware/Camera.Parameters.html#FLASH_MODE_TORCH
